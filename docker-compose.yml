version: '3.8'

services:
  db:
    image: mysql:8.0
    restart: unless-stopped
    env_file: ./.env
    environment:
      - MYSQL_ROOT_PASSWORD=$MYSQLDB_ROOT_PASSWORD
      - MYSQL_DATABASE=$MYSQLDB_DATABASE
    ports:
      # map host local port (e.g. 3307) to the container's MySQL port 3306
      - $MYSQLDB_LOCAL_PORT:3306
    volumes:
      - db:/var/lib/mysql

  server:
    depends_on:
      - db
    build: ./server
    restart: unless-stopped
    env_file: ./.env
    ports:
      - $SERVER_LOCAL_PORT:$SERVER_DOCKER_PORT
    environment:
      - DB_HOST=db
      - DB_USER=$MYSQLDB_USER
      - DB_PASSWORD=$MYSQLDB_ROOT_PASSWORD
      - DB_NAME=$MYSQLDB_DATABASE
      - DB_PORT=3306
    stdin_open: true
    tty: true

  client:
    build: ./client
    restart: unless-stopped
    env_file: ./.env
    environment:
      - CHOKIDAR_USEPOLLING=true
      - HOST=0.0.0.0
    ports:
      - $CLIENT_LOCAL_PORT:$CLIENT_DOCKER_PORT
    depends_on:
      - server
    stdin_open: true
    tty: true

volumes: 
  db:
